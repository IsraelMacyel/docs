---
title: "How to host your Uptime Kuma"
---

# How to host your Uptime Kuma

> This tutorial will guide you to create and deploy your Uptime Kuma application on Virtus Cloud.

## Introducción

* This article guides you through creating and hosting an Uptime Kuma application on Virtus Cloud.
* Before getting started, make sure you have an account on Virtus Cloud, which can be done through the sign up page. You can use your email to create an account.
* Finally, you need to have an active paid plan on your account. You can view our plans and purchase one according to your needs [here](https://virtuscloud.app/en/pricing).

## Choosing a version

* First, we need to choose a version available [here](https://github.com/virtuscloud-education/uptimekuma-web/releases).
* Download the `project.zip` of the choosed version.

## Config file

* If you want a custom subdomain instead of a random hash, you can modify your virtuscloud.config to set the desired subdomain.

```systemd virtuscloud.config theme={null}
DISPLAY_NAME=Uptime Kuma
MEMORY=1024
VERSION=recommended
AUTORESTART=true
MAIN=server/server.js
SUBDOMAIN=my-uptime-kuma
```

<Note>You can also set this through the upload menu.</Note>

## Uploading the Project to Virtus Cloud

Después de preparar los archivos del proyecto, ahora puedes subirlos a Virtus Cloud y alojar tu proyecto.

<Tabs>
  <Tab title="Upload via Dashboard">
    Access the [Virtus Cloud Dashboard](https://virtuscloud.app/en/upload) and upload your project files.

    <Frame>
      <img src="https://cdn.virtuscloud.app/docs/articles/dashboard/uploading.gif" alt="Uploading application to Virtus Cloud" style={{ borderRadius: "0.2rem" }} />
    </Frame>
  </Tab>

  <Tab title="Upload via CLI">
    <Steps>
      <Step title="First Step">
        First, you need to have the CLI installed in your environment. If you don't have it yet, run the following command in your terminal:

        ```
        npm install -g @virtuscloud/cli
        ```

        If you already have it, we recommend updating it. To do this, run the following command in your terminal:

        <Tabs>
          <Tab title="Windows">
            ```bash theme={null}
            virtuscloud update
            ```
          </Tab>

          <Tab title="Linux, macOS, and WSL">
            ```bash theme={null}
            curl -fsSL https://cli.virtuscloud.app/install | bash
            ```
          </Tab>
        </Tabs>
      </Step>

      <Step title="Second Step">
        Ahora, para autenticarte y usar otros comandos de la CLI, encontrarás tu clave de autorización [aquí](https://virtuscloud.app/es/account/security) by clicking on "Request API Key". After obtaining your authorization key, run the following command:

        ```bash theme={null}
        virtuscloud auth login
        ```
      </Step>

      <Step title="Third Step">
        Finally, to deploy your application to Virtus Cloud using the CLI, you need to run the following command, passing the path to your zip file:

        ```bash theme={null}
        virtuscloud upload zip
        ```
      </Step>
    </Steps>
  </Tab>
</Tabs>
